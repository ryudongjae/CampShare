spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campshare?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 0070
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
  redis:
    port: 6379
    host: 127.0.0.1

  mail:
      host: smtp.gmail.com
      port: 587
      username:
      password:
      properties:
        mail.smtp.auth: true
        mail.smtp.starttls.enable: true

logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace